<% if (current_user == post.user) || (current_user.orders.exists?(post_number: post.id)) %>
  <%= render 'admins/template/poss_special_posts_index', post: post %>
<% else %>
  <%= render 'admins/template/before_poss_special_posts_index', post: post %>
<% end %>