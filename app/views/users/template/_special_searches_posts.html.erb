<% if (current_user == post.user) || (current_user.orders.exists?(post_number: post.id)) %>
              <%= render 'template/poss_special_searches_posts', post: post %>
            <% else %>
              <%= render 'template/before_poss_special_searches_posts', post: post %>
            <% end %>